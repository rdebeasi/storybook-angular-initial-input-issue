# Greeter story is broken. 

- Expect: "Hello Dude!"
- Actual: "Hello World!" 

The correct behavior can be seen when running `ng serve`.

`ngOnChanges` is correctly called though, but the view is not updated.
